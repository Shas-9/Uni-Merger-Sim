### How to activate the script
**For the first time only**, create a venv environment
```
python3 -m venv project_venv
pip install osmnx
pip install matplotlib
```

Activate
```
source project_venv/bin/activate
```

